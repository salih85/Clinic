<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Prescription Billing</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
</head>

<body class="bg-light">

<%- include('navber') %>

<div class="container mt-4">

    <h2 class="text-primary">Prescription Details</h2>

    <div class="card mt-3 shadow-sm">
        <div class="card-header bg-info text-white">
            Token: <%= consultation.token %>
        </div>

        <div class="card-body">
            <p><strong>Patient:</strong> <%= consultation.patient %></p>
            <p><strong>Problem:</strong> <%= consultation.topic %></p>
            <p><strong>Prescription:</strong> <%= consultation.prescription %></p>

            <hr>
            <form action="/pharm/consultations/update/<%= consultation._id %>" method="POST">

                <div class="form-group">
                    <label><strong>Billing Amount (₹)</strong></label>
                    <input type="number" name="amount" class="form-control"
                           value="<%= consultation.prescriptionAmount || '' %>" required>
                </div>

                <button class="btn btn-success mt-2">Mark as Purchased</button>
                <a href="/pharm/consultations" class="btn btn-secondary mt-2">Back</a>

            </form>
        </div>
    </div>

</div>

</body>
</html>
